// This file contains constants related to cookies used in the application.
// It is important to keep these constants in a separate file for better organization and maintainability.

const COOKIE_OPTIONS = {
  httpOnly: true,
  secure: true,
};

export { COOKIE_OPTIONS };
